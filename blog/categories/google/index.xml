<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom"><channel><title>Google on 大白鲸笔记本</title><link>https://chen-zhe.github.io/blog/categories/google/</link><description>Recent content in Google on 大白鲸笔记本</description><generator>Hugo -- gohugo.io</generator><language>en-us</language><lastBuildDate>Thu, 19 Feb 2026 00:00:00 +0000</lastBuildDate><atom:link href="https://chen-zhe.github.io/blog/categories/google/index.xml" rel="self" type="application/rss+xml"/><item><title>Google's Developer Infrastructure is Unmatched</title><link>https://chen-zhe.github.io/blog/p/googles-developer-infrastructure-is-unmatched/</link><pubDate>Thu, 19 Feb 2026 00:00:00 +0000</pubDate><guid>https://chen-zhe.github.io/blog/p/googles-developer-infrastructure-is-unmatched/</guid><description>&lt;p&gt;Everything I wrote down here has already been discussed extensively on the Internet. The following post is just my experience on them.&lt;/p&gt;
&lt;h2 id="piper-and-cl-change-list"&gt;Piper and CL (Change List)
&lt;/h2&gt;&lt;p&gt;While Piper is not the only code repository at Google (Google has a Git repo for open-source projects like Android OS and Chrome), it&amp;rsquo;s the biggest and mostly used one. It stores all kinds of internal code, from &lt;code&gt;experimental/users&lt;/code&gt;&amp;rsquo; random scripts to &lt;code&gt;learning/gemini&lt;/code&gt;&amp;rsquo;s SoTA architecture.&lt;/p&gt;
&lt;p&gt;Piper has its own interface &lt;code&gt;p4&lt;/code&gt; interface for version control, but I still prefer the &amp;ldquo;close-to-git&amp;rdquo; mercury-like &lt;code&gt;fig&lt;/code&gt; interface. Similar to Git&amp;rsquo;s branch, I need to create a new workspace in order to commit code, &lt;code&gt;p4head&lt;/code&gt; is a CL number indicating the latest commit in the branch. After you make the changes, you upload them into a Change List (CL). Internally, &lt;code&gt;fig&lt;/code&gt; would create another new workspace with all the changes and create a CL with the native Piper interface.&lt;/p&gt;
&lt;p&gt;Fun fact, the command to submit a CL in &lt;code&gt;fig&lt;/code&gt; is &lt;code&gt;hg submit &amp;lt;cl number&amp;gt;&lt;/code&gt; and can be easily mistyped as &lt;code&gt;hg sumbit &amp;lt;cl number&amp;gt;&lt;/code&gt;. &lt;code&gt;hg&lt;/code&gt; will actually convert the CL number into binary, sum the bits and print it out!&lt;/p&gt;
&lt;p&gt;Each CL is given a unique monotonically increasing number (OCL number) at upload, so that you can send it for review, and another one when the CL gets merged. This means committed CL numbers are always monotonically increasing. This has some convenient properties:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;If a fix is submitted as cl/12345, I just need to sync past cl/12345 in my workspace to get the fix. No more reading of commit history, which is infeasible anyway, given the number of CLs humans and bots submits in a minute.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;If a high-impact bug is introduced at cl/123 and fixed at cl/456, then you just need to mark [123, 456] as a bad CL range. Internal tools will recognize it and prevent you from running code in production machines built in the bad CL ranges.&lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Unlike Git, any further changes you make to the workspace after the initial commit is amended to the same commit (with a different hash), effectively doing Git&amp;rsquo;s squash-and-merge on the fly.&lt;/p&gt;
&lt;h2 id="citc-code-in-the-cloud-and-cider-v"&gt;CitC (Code in the Cloud) and Cider-V
&lt;/h2&gt;&lt;p&gt;In my mind, CitC is a paradigm-shifting concept, and probably also inspired the collaborative Google docs. I can code, test and submit the change all without using a dedicated developer instance. Similar to a network file system, CitC virtually mounts the whole Piper codebase in a directory with a write-on-copy concept, so only my changes to files are stored somewhere outside piper. It&amp;rsquo;s easy to see that this doesn&amp;rsquo;t require a lot of dedicated resources and the entire codebase can be hosted together with an web-based editor. It also allows collaborated editing of the same file at the same time, with some delays! I personally never tried it as pair coding is still rare.&lt;/p&gt;
&lt;p&gt;The hosted editor used to be Cider, an internally built editor. Luckily for me, when I joined Google in 2022, Cider-V, a modified VS Code editor, is in beta. So I took the familiar option and never looked back.&lt;/p&gt;
&lt;h2 id="testing"&gt;Testing
&lt;/h2&gt;&lt;p&gt;Testing is in Google&amp;rsquo;s core engineering culture, so we have multiple testing infrastructure available:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;presumbit run the directly affected unit tests&lt;/li&gt;
&lt;li&gt;TAP (Test Automation Platform) presubmit discovers dependencies from other directories and runs those unit tests, too. If a test becomes flaky, Presubmit advisor can automatically ignore them. You can manually put a CL into a TAP train, which runs in an infinitely loop, grouping multiple CLs together and runs every unit test in the codebase to make sure nothing breaks.&lt;/li&gt;
&lt;li&gt;Guitar tests runs longer-running integration tests. It&amp;rsquo;s configured in each directory&amp;rsquo;s blueprints.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;It seems that Google&amp;rsquo;s test cases not only outputs regularly to stdout, but also outputs a parsable YAML file that a web service can read. This is true even for tests running locally on the developer instance (I used a virtual Cloudtop). The web service can present test results nicely, breaking the test assertions outputs into individual test cases and present all test run logs in a separate tab. It would also aggregate test statistics for the presubmit advisor.&lt;/p&gt;
&lt;h2 id="blaze-build-and-hermetic-python"&gt;Blaze BUILD and Hermetic Python
&lt;/h2&gt;&lt;p&gt;Blaze, open sourced as &lt;a class="link" href="https://bazel.build/" target="_blank" rel="noopener"
&gt;Bazel&lt;/a&gt;, is our internal build system that supports every language Google uses, from C++, Java, to Python. It&amp;rsquo;s sometimes annoying to write the build rules, but it bring a benefit: hermetic builds. The build engine will traverse through the dependency graph and build files from any language in their intend ways, and of course, it detects cyclic dependencies. Conceptually, it&amp;rsquo;s very similar to Java&amp;rsquo;s Uber Jar, but extended to all languages. Even Python is built this way, so no more virtual environments or docker images.&lt;/p&gt;
&lt;p&gt;Want to use open-source code? Since the build is hermetic to Piper, there are teams dedicated to importing popular open-source projects (e.g. &lt;code&gt;numpy&lt;/code&gt;) into Piper under the &lt;code&gt;/third_party/&lt;/code&gt; directory and making sure that nothing breaks after each update. Our build rule will recognize the &lt;code&gt;third_party&lt;/code&gt; prefix and build the hermetic Python executable such that importing those libraries is the same as how non-Googlers use them.&lt;/p&gt;
&lt;h2 id="borg-and-xmanager"&gt;Borg and XManager
&lt;/h2&gt;&lt;p&gt;Since all machines at Google runs the same gLinux distribution and contains the same dependencies, we can execute any executables on any machine, bare metal. Borg, is like the Kubernetes, but managing programs running on bare metal machines, no hypervisor needed.&lt;/p&gt;
&lt;p&gt;&lt;a class="link" href="https://github.com/google-deepmind/xmanager" target="_blank" rel="noopener"
&gt;XManger&lt;/a&gt; was originally developed by DeepMind to manage training runs and TPU allocations. Its graphical interface is much better than Borg&amp;rsquo;s ancient look. Now, it&amp;rsquo;s platform to manage dynamic TPU allocations (thinking AWS Spot instance), hosts and manages training runs. Each training run is uniquely identified by a XID and can have multiple worker units, some worker units using TPU and some worker units using only CPU.&lt;/p&gt;
&lt;p&gt;Logs can viewed and searched in an interface very similar to GCP&amp;rsquo;s log viewer. It&amp;rsquo;s fast and powerful, supporting log level filters and fuzzy matches.&lt;/p&gt;</description></item></channel></rss>